---
- name: UFW - Enable and deny by default
  community.general.ufw:
    state: enabled
    default: deny

- name: UFW - Allow SSH connections
  community.general.ufw:
    rule: allow
    name: OpenSSH
